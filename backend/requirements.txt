fastapi==0.115.6
uvicorn[standard]==0.34.0
motor==3.6.0
pymongo==4.9.2
python-multipart==0.0.20
python-dotenv==1.0.1
google-genai>=2.22.0
pydantic==2.13.5

# ASR (app/speech2text.py, ASR_PROVIDER=provided). Offline, no API key.
# Pulls in ctranslate2 (inference) and av/PyAV (audio decode - bundles its
# own ffmpeg, no system ffmpeg needed).
faster-whisper==1.2.1


# Actian VectorAI DB client (app/vectordb.py). The semantic memory layer -
# memories are embedded on write and retrieved by similarity on read.
actian-vectorai-client==1.0.1